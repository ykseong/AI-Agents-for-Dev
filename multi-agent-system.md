# 프로그램 개발을 위한 멀티 에이전트 협업 시스템

## 개요

단일 에이전트의 한계를 극복하고 각 전문 영역에 집중할 수 있도록 역할이 분리된 협업 구조의 멀티 에이전트 시스템이다. 각 에이전트는 명확한 책임 범위를 가지며, 구조화된 산출물을 통해 다음 에이전트로 작업을 인계한다.

---

## 에이전트 전체 구성

| 에이전트 | 역할 요약 | 주요 입력 | 주요 출력 |
|---|---|---|---|
| PL Agent | 요구사항 분석, 프로젝트 기획 및 설계 | 사용자 요청 | 기획서, 설계 문서 |
| Architecture Agent | 시스템 구조 및 기술 스택 결정 | PL 설계 문서 | 아키텍처 명세 |
| UX Agent | UI/UX 설계 및 프로토타입 | PL 기획서 | 디자인 명세, 와이어프레임 |
| Coding Agent | 실제 코드 구현 | 설계 문서, 디자인 명세 | 소스 코드 |
| QA Agent | 테스트 설계 및 실행, 버그 추적 | 소스 코드, 요구사항 | 테스트 결과, 버그 리포트 |
| Review Agent | 코드 품질, 성능, 보안 검토 | 소스 코드 | 리뷰 리포트 |
| DevOps Agent | 빌드, 배포, 인프라 관리 | 소스 코드, 배포 설정 | 배포 환경, CI/CD 파이프라인 |
| Documentation Agent | 기술 문서 및 사용자 문서 작성 | 모든 에이전트 산출물 | 문서 산출물 |
| Database Agent | 데이터 모델 설계 및 쿼리 최적화 | 아키텍처 명세 | DB 스키마, 마이그레이션 스크립트 |

---

## 에이전트 간 협업 흐름

```
사용자 요청
    │
    ▼
┌─────────────┐
│  PL Agent   │ ── 요구사항 분석, 프로젝트 기획, 일정 수립
└──────┬──────┘
       │ 기획서 / 기능 명세
       ├──────────────────────┬───────────────────────┐
       ▼                      ▼                       ▼
┌──────────────┐     ┌─────────────────┐     ┌──────────────┐
│Architecture  │     │   UX Agent      │     │  Database    │
│   Agent      │     │                 │     │   Agent      │
└──────┬───────┘     └────────┬────────┘     └──────┬───────┘
       │ 아키텍처 명세          │ 디자인 명세            │ DB 스키마
       └──────────────────────┼───────────────────────┘
                              ▼
                     ┌──────────────┐
                     │ Coding Agent │ ◄─── 버그 수정 요청 (QA / Review)
                     └──────┬───────┘
                            │ 소스 코드
               ┌────────────┼────────────┐
               ▼            ▼            ▼
       ┌──────────┐  ┌──────────┐  ┌──────────────┐
       │ QA Agent │  │  Review  │  │ DevOps Agent │
       │          │  │  Agent   │  │              │
       └────┬─────┘  └────┬─────┘  └──────┬───────┘
            │             │               │
            └──────┬───────┘               │ 배포
                   ▼                       ▼
          버그/개선 요청          ┌─────────────────────┐
          → Coding Agent         │  Documentation Agent│
                                 └─────────────────────┘
```

---

## 에이전트별 상세 역할 정의

---

### 1. PL Agent (Project Lead)

**책임 범위**: 프로젝트 전체 기획 및 조율

#### 역할
- 사용자의 요구사항을 수집하고 구체적인 기능 목록(Feature List)으로 정리
- 기능별 우선순위 책정 및 개발 단계(Phase) 분할
- 전체 프로젝트 일정 수립 및 마일스톤 정의
- 기술적으로 모호한 요구사항에 대해 사용자에게 추가 질의
- 각 에이전트에게 작업 지시를 내리고 진행 상황을 취합
- 에이전트 간 충돌 또는 의존성 이슈 발생 시 우선순위 결정

#### 산출물
- `requirements.md`: 기능 요구사항 명세서
- `project-plan.md`: 개발 계획서 (일정, 마일스톤, 담당 에이전트)
- `task-breakdown.md`: 에이전트별 작업 지시서

---

### 2. Architecture Agent

**책임 범위**: 시스템 구조 설계 및 기술 스택 결정

#### 역할
- PL Agent의 요구사항을 바탕으로 전체 시스템 아키텍처 설계
- 기술 스택 선정 (언어, 프레임워크, 라이브러리, 클라우드 서비스 등)
- 서비스 간 인터페이스 및 API 계약 정의 (REST, GraphQL, gRPC 등)
- 모놀리식 vs 마이크로서비스 등 아키텍처 패턴 결정
- 확장성, 가용성, 유지보수성 관점의 트레이드오프 검토
- Coding Agent 및 Database Agent가 따라야 할 설계 원칙 수립

#### 산출물
- `architecture.md`: 시스템 아키텍처 문서 (다이어그램 포함)
- `tech-stack.md`: 기술 스택 선정 이유 및 버전 명세
- `api-contract.md`: API 엔드포인트 및 인터페이스 명세

---

### 3. UX Agent (UI/UX Designer)

**책임 범위**: 사용자 경험 설계 및 UI 명세 작성

#### 역할
- 사용자 페르소나 및 사용자 여정 지도(User Journey Map) 작성
- 화면 흐름(Screen Flow) 및 정보 구조(IA) 설계
- 각 화면의 와이어프레임 및 UI 컴포넌트 명세 작성
- 디자인 시스템 정의 (색상, 타이포그래피, 간격, 컴포넌트 규칙)
- 접근성(Accessibility) 기준 준수 여부 검토
- Coding Agent가 구현한 UI가 설계 명세와 일치하는지 검수

#### 산출물
- `ux-flow.md`: 사용자 흐름도 및 화면 전환 정의
- `wireframe/`: 각 화면별 와이어프레임 (텍스트 또는 이미지)
- `design-system.md`: 디자인 토큰 및 컴포넌트 가이드라인

---

### 4. Coding Agent

**책임 범위**: 실제 소스 코드 구현

#### 역할
- Architecture Agent의 설계 문서를 기반으로 백엔드/프론트엔드 코드 구현
- UX Agent의 디자인 명세에 따라 UI 컴포넌트 구현
- Database Agent의 스키마를 기반으로 데이터 접근 레이어 구현
- 코드 구현 시 Architecture Agent가 정의한 설계 원칙 준수
- QA Agent 및 Review Agent의 피드백을 수령하고 수정 사항 반영
- 기능 구현 완료 후 관련 에이전트에 검토 요청

#### 산출물
- 소스 코드 (기능별 커밋 단위 구분)
- 인라인 코드 주석 (복잡한 로직에 한정)

---

### 5. QA Agent (Quality Assurance)

**책임 범위**: 테스트 설계, 실행, 버그 추적

#### 역할
- 요구사항 명세를 기반으로 테스트 케이스 작성
- 단위 테스트(Unit Test) 코드 작성 및 실행
- 통합 테스트(Integration Test) 및 E2E 테스트 실행
- 회귀 테스트(Regression Test)를 통해 기존 기능의 파손 여부 확인
- 발견된 버그를 재현 가능한 형태로 정리하여 Coding Agent에 전달
- 버그 수정 후 재테스트(Retest) 및 수정 확인

#### 산출물
- `test-cases.md`: 기능별 테스트 케이스 목록
- `test-results.md`: 테스트 실행 결과 및 커버리지 리포트
- `bug-report.md`: 버그 ID, 재현 절차, 예상 동작, 실제 동작, 심각도

---

### 6. Review Agent (Code Reviewer)

**책임 범위**: 코드 품질, 성능, 보안 검토

#### 역할
- 코드 스타일 및 컨벤션 준수 여부 확인 (린팅 규칙, 네이밍, 포맷팅)
- 중복 코드, 복잡도 과잉, 불필요한 의존성 등 리팩토링 필요 지점 식별
- 알고리즘 및 쿼리의 성능 병목 가능성 검토
- OWASP Top 10 기반 보안 취약점 탐지 (인젝션, 인증 결함, 민감 데이터 노출 등)
- 하드코딩된 시크릿, 환경 변수 미처리 등 보안 안티패턴 식별
- 검토 결과를 우선순위와 함께 Coding Agent에 전달

#### 산출물
- `review-report.md`: 지적 사항 목록 (파일명, 라인 번호, 심각도, 개선 제안)

---

### 7. DevOps Agent

**책임 범위**: 빌드 자동화, CI/CD, 인프라 관리

#### 역할
- CI/CD 파이프라인 구성 (빌드 → 테스트 → 배포 자동화)
- 개발(dev) / 스테이징(staging) / 운영(production) 환경 구성 및 관리
- 컨테이너화(Docker) 및 오케스트레이션(Kubernetes) 설정
- 환경 변수 및 시크릿 관리 전략 수립
- 모니터링 및 알림 설정 (로그 수집, 지표, 이상 탐지)
- 배포 롤백 전략 및 운영 장애 대응 절차 정의

#### 산출물
- `ci-cd-pipeline.md`: 파이프라인 구성 및 단계별 동작 설명
- CI/CD 설정 파일 (`.github/workflows/`, `Dockerfile`, `docker-compose.yml` 등)
- `infra.md`: 인프라 구성 및 환경별 설정 명세

---

### 8. Documentation Agent

**책임 범위**: 기술 문서 및 사용자 문서 생성·유지

#### 역할
- API 문서 자동 생성 및 유지 관리 (OpenAPI/Swagger 등)
- 개발자를 위한 README, 설정 가이드, 기여 가이드 작성
- 최종 사용자를 위한 사용 설명서 및 FAQ 작성
- 변경 이력 관리 (CHANGELOG) 및 릴리즈 노트 작성
- 코드베이스 내 복잡한 모듈의 설명 문서 작성
- 문서와 실제 코드 간 불일치 발생 시 최신화

#### 산출물
- `README.md`: 프로젝트 개요, 설치, 실행 방법
- `docs/api/`: API 레퍼런스 문서
- `docs/user-guide/`: 사용자 가이드
- `CHANGELOG.md`: 버전별 변경 이력

---

### 9. Database Agent

**책임 범위**: 데이터 모델 설계 및 데이터 레이어 최적화

#### 역할
- 요구사항을 기반으로 ERD(Entity-Relationship Diagram) 및 DB 스키마 설계
- 정규화 수준 결정 및 인덱스 전략 수립
- 마이그레이션 스크립트 작성 및 버전 관리
- 쿼리 성능 분석 및 최적화 제안 (실행 계획 검토, N+1 문제 등)
- 데이터 보존 정책, 백업 전략, 접근 권한 설계
- RDBMS / NoSQL 선택 근거 제시 및 데이터 일관성 전략 수립

#### 산출물
- `schema.sql` (또는 ORM 모델 파일): 테이블 및 관계 정의
- `migrations/`: 버전별 마이그레이션 스크립트
- `db-design.md`: ERD, 인덱스 전략, 설계 근거

---

## 에이전트 간 커뮤니케이션 규칙

1. **구조화된 산출물 전달**: 에이전트 간 인계는 반드시 문서 형태의 산출물로 이루어진다. 암묵적 맥락에 의존하지 않는다.
2. **단방향 책임 원칙**: 각 에이전트는 자신의 책임 범위 밖의 결정을 독단적으로 내리지 않는다. 범위를 벗어난 이슈는 PL Agent에 에스컬레이션한다.
3. **피드백 루프 명확화**: QA Agent와 Review Agent는 Coding Agent에 수정 요청 시, 파일명·라인 번호·재현 방법을 포함한 정형화된 리포트를 전달한다.
4. **변경 전파 원칙**: 아키텍처나 스키마 변경이 발생할 경우, Architecture Agent 또는 Database Agent는 영향받는 모든 에이전트에 변경 내용을 전파한다.
5. **완료 기준 준수**: 각 에이전트는 자신의 산출물이 정의된 완료 기준(Definition of Done)을 충족했을 때에만 다음 에이전트에 작업을 인계한다.

---

## 주요 시나리오별 에이전트 협업 순서

### 신규 기능 개발
```
PL → Architecture → UX + Database (병렬) → Coding → QA + Review (병렬) → DevOps → Documentation
```

### 버그 수정
```
QA (버그 발견) → PL (우선순위 결정) → Coding (수정) → QA (재테스트) → DevOps (패치 배포)
```

### 성능 개선
```
Review (병목 탐지) → Architecture (개선 방안) → Database + Coding (병렬 최적화) → QA (성능 테스트) → DevOps (배포)
```

### 보안 취약점 대응
```
Review (취약점 식별) → PL (긴급도 결정) → Coding (수정) → QA (보안 테스트) → DevOps (긴급 배포)
```
